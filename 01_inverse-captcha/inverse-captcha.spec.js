describe('Inverse Captcha', function() {
  let inverseCaptcha;

  beforeEach(function() {
    inverseCaptcha = new InverseCaptcha();
  });

  it('test', function() {
    inverseCaptcha.decode();
  });
});
